#include <stdio.h>
/**
 * main - Entry point
 * descrption:'prints all single digit numbers of base ten'
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 0; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
